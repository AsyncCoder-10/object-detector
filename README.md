# object-detector
Object detection app using Hugging Face's DETR (facebook/detr-resnet-50) with a Gradio UI — upload an image and get bounding boxes, labels, and confidence scores for detected objects.
